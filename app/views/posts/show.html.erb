<p>
<%= @post.message %>
</p>

Add a comment
<%= form_for([@post, @post.comments.build]) do |form| %>
  <p>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit "Post comment" %>
  </p>
<% end %>
